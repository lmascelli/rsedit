#+author: Leonardo Mascelli
#+title: rsedit

* Fixes todo
- [ ] Having a buffer opened in two windows and move the cursor in one window
      makes the view of the other window move too.
- [ ] Pasting multiple lines keep the line on a single row.

* Macro todo 
- [ ] Make electric-pair's balance check cheaper than a scan to the end of the
      buffer. With the checkpoints above, everything that asks about *point* is
      now nearly free, but `(syntax-ppss (point-max))' still costs the distance
      from the edit to the end of the file on every self-insert. Most keystrokes
      do not change the tail's net depth at all, so there is probably an
      invariant to cache there -- or a cheaper question for electric-pair to
      ask than whether the whole buffer balances.
- [ ] The editor structure is becoming too complex. I want to create compartiment
      that manage single aspects of the editor like buffers, layout, searches, commands,
      etc... so that a fail like the previous one where two windows where associated
      to the same buffer and one of them is closed cannot leave the other one with a
      pending reference to that buffer because the behavior of closing a window is 
      clearly managed by the layout manager. it is just a refactoring of the system.
- [ ] LSP integration
- [ ] Integrated terminal emulation

