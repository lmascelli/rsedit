#+author: Leonardo Mascelli
#+title: rsedit

* Fixes todo
- [ ] C-x u still doens't ask to repeat u to do further undos
- [ ] rsedits_pages_are_offered_for_completion still fail probably for OutOfFuel 
- [ ] electric-pair doesn't recognize a match pair in some case for example:

fn a() {
  body function
}

deleting the first { and inserting it against brings to a couple {} and the second
one can't be removed without removing also the first. When a pair match the corrispective
closed one the closing one shouldn't be added. The same is valid for inserting a closing
one that match an already inserted open pair

* Macro todo 
- [ ] LSP integration
- [ ] Integrated terminal emulation

